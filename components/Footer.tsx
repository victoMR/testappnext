import React from "react";
import styles from "@/styles/styles.module.css";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <div className="container">
                <span className="text-muted">Derechos reservados | 2024.</span>
            </div>
        </footer>
    );
}

export default Footer;
